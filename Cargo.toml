[package]
name = "bdat"
version = "0.1.0"
edition = "2021"
license = "mit OR apache-2.0"

[dependencies]
serde = "1.0"
byteorder = "1.4"
thiserror = "1.0"
enum-kinds = "0.5.1"
num_enum = "0.5.7"

[features]
derive-impls = ["serde/derive"]